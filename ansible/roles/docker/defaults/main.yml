---

docker_architecture: "{{ {'x86_64': 'amd64', 'i386': '386', 'armv7l': 'armhf', 'aarch64': 'arm64'}[ansible_facts.architecture] | default(ansible_facts.architecture) }}"
docker_daemon_config_path: "/etc/docker/daemon.json"
